<!DOCTYPE html>
<html>
  <head>
    <title>The Trainer In Touch</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if notice %>
      <p class='alert alert-success'><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class='alert alert-danger'><%= alert %></p>
    <% end %>
    <% if current_user %>
      <p>
        Hi, <%= current_user.first_name %>
      </p>
      <%= button_to 'Log Out', destroy_user_session_path, method: :delete, data: { turbo: 'false' }, class: 'font-medium text-indigo-600 hover:text-indigo-500' %>
    <% else %>
      <%= link_to 'Sign In', new_user_session_path, class: 'font-medium text-indigo-600 hover:text-indigo-500' %>
    <% end %>
    <main><%= yield %></main>
  </body>
</html>
